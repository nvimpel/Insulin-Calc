The purpose is to use data from CGM(Continuous glucose monitoring) to the convert them into a table, and add apropriate insulin values.
The old python scripts are junk.
for 30k rows Python runtime was 15-20 mins
c++ manages the same task in no time
