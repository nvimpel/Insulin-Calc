The purpose is to use data from CGM(Continuous glucose monitoring) to convert them into a table and add appropriate insulin values. \n
The old Python scripts are junk. \n
for 30k rows Python runtime was 15-20 mins z \n
c++ manages the same task in no time
